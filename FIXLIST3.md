# TubeExtractor 修复清单 · 第三轮（Web 前端错误处理）

> 背景：用户实际使用时把**视频标题**（非链接）粘进输入框，服务端正确返回 400，但前端 SSE 解析器把非事件流响应静默吞掉——页面永远停在「正在解析... 0%」，无任何提示。根因：此前只用 curl 验证过 400 响应本身，未验证前端对该响应的处理路径。

## H1. 前端输入预校验 ✅
- [x] 提交前校验 URL：无 `http(s)://` 前缀、含空格（标题/搜索词特征）时页面内直接给出明确提示（含"分享→复制链接"指引），不发起请求。
- [x] 自动补全：无协议但形如域名的输入（`www.bilibili.com/...`）自动补 `https://` 并回填输入框。

## H2. 前端非 SSE 响应的错误展示 ✅
- [x] `fetch` 后先检查 `response.ok` 与 content-type：非 `text/event-stream` 时读取错误信息并显示在页面（❌ + 收起进度浮窗），不再进入 SSE 解析循环。

## H3. SSE 连接异常结束的兜底 ✅
- [x] `taskFinished` 标记：收到 `success`/`error` 终态时置位；连接结束但未收到终态时显示"连接已断开，任务可能未完成"。
- [x] 网络异常（fetch 抛错）显示 `❌ 请求失败: ...` 并收起浮窗；校验类错误用页面内 `showValidationError` 替代 alert。

## 端到端验证（真实浏览器 IAB 实测）✅

| # | 场景 | 结果 |
| --- | --- | --- |
| A | 粘贴视频标题（复现用户操作）→ 点击 | ✅ 页面立即显示 ❌ "输入的不是链接…"，不再卡死 |
| B | 粘贴 `www.bilibili.com/video/...`（无协议） | ✅ 自动补全为 `https://...`，任务启动；B 站返回 412 风控时错误同样清晰展示 |
| C | 有效 YouTube 链接完整流程 | ✅ "博主：Rick Astley (已完成，成功 1/1)" + 绿色"同步成功"卡片，文件落盘 |

## 附带发现：陈旧的常驻服务进程 ⚠️

浏览器验证时发现，响应请求的其实是用户数小时前启动、**一直未重启的旧版 `npm start` 进程**（证据：新写入文件的 frontmatter 无 `transcript:` 字段；前端文件从磁盘实时读取所以新前端逻辑生效，但旧后端没有 ASR/来源卡片/`OUTPUT_DIR` 环境变量支持）。已用 `pkill` 停掉该进程。**用户需重新 `npm start` 以加载全部新后端行为**；`OUTPUT_DIR` 环境变量经新版 server 实测正常（`/tmp/tube-dircheck` 写入验证通过）。
